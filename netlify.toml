# netlify.toml
[build]
  command = "npm ci && npm run build"
  publish = "dist"

[build.environment]
  NODE_VERSION = "20"

# SPA fallback so refresh/deep links don't 404
[[redirects]]
  from = "/*"
  to = "/index.html"
  status = 200
